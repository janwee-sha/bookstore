server:
  port: 7000

spring:
  application:
    name: config-server
  profiles:
    default: native
  cloud:
    config:
      server:
        native:
          search-locations: classpath:/config

encrypt:
  key: j@NwEes_KeY
