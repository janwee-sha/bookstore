spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          jwk-set-uri: http://authorization:18083/oauth2/jwks

eureka:
  instance:
    hostname: gateway
  client:
    service-url:
      defaultZone: http://eureka:18081/eureka/
